<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <PreferenceCategory
        app:title="Notification Settings">



        <Preference
            app:key="notification_settings_link"  app:title="Notification Settings"
            app:summary="Open system notification settings for this app"
            app:persistent="false"/>

        <!-- Snooze Duration for reminders -->
        <ListPreference
            app:key="snooze_duration"
            app:title="Snooze Duration"
            app:summary="Select the snooze time for medication reminders"
            app:entries="@array/snooze_duration_entries"
            app:entryValues="@array/snooze_duration_values"
            app:defaultValue="5"
            app:useSimpleSummaryProvider="true" />

        <!-- Enable Backup Reminder -->
        <SwitchPreferenceCompat
            app:key="enable_backup_reminder"
            app:title="Enable Backup Reminder"
            app:summary="Enable a follow-up reminder if a dose is missed"
            app:defaultValue="true" />

        <!-- Backup Reminder Delay -->
        <ListPreference
            app:key="backup_reminder_delay"
            app:title="Backup Reminder Delay"
            app:summary="Select how long to wait before sending a backup reminder"
            app:entries="@array/backup_reminder_delay_entries"
            app:entryValues="@array/backup_reminder_delay_values"
            app:defaultValue="30"
            app:useSimpleSummaryProvider="true" />

    </PreferenceCategory>







</PreferenceScreen>
